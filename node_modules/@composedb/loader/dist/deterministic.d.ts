import type { GenesisCommit } from '@ceramicnetwork/common';
import { StreamID } from '@ceramicnetwork/streamid';
import type { LoadKey } from './types.js';
export type GenesisMetadata = {
    controller: string;
    model: StreamID | string;
    unique?: Array<string>;
};
/**
 * Create a deterministic genesis commit matching the logic unsed internally by ModelInstanceDocument.
 */
export declare function createDeterministicGenesis(meta: GenesisMetadata): GenesisCommit;
/**
 * Create a LoadKey for a deterministic stream.
 */
export declare function createDeterministicKey(meta: GenesisMetadata): Promise<LoadKey>;
/**
 * Get the cache key for a deterministic stream.
 */
export declare function getDeterministicCacheKey(meta: GenesisMetadata): string;
